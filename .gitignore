# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# Node / frontend
node_modules/
dist/
.vite/
.next/
*.local

# Env / secrets — catch all .env files anywhere in the repo (root and frontend/)
**/.env
**/.env.*
.env*.bak
*.pem
fernet.key

# OS / editor
.DS_Store
*.log
.idea/
.vscode/

# Docker volumes (local)
.postgres-data/
.redis-data/
.n8n-data/

# Local workspace/runtime artifacts
AGENTS.md
outputs/
backend/celerybeat-schedule

# Throwaway local SQLite databases (the no-Docker dev path — see README).
*.db
*.db-journal

# Generated test fixtures. Not committed on purpose: scoring is recency-based, so
# absolute dates rot — regenerate with app.scripts.make_test_data instead.
testdata/

# Standalone landing-page export (npm run preview:export) — build output
frontend/.preview-dist/
churnary-landing-preview.html
